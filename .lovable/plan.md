Em `src/pages/Index.tsx`, no Hero, adicionar um CTA secundário **"Resultados de exames"** ao lado do botão "Agendar exame", linkando para `https://web.clinux.com.br/portal/medimagemsm/resultados` (target `_blank`).

Estilo: `Button` `size="lg"` com `variant="outline"` e classes `border-primary-foreground text-primary-foreground hover:bg-primary-foreground/10 font-semibold text-base px-8` (mesmo padrão visual do antigo botão "Fale com a equipe", para manter contraste sobre o hero).